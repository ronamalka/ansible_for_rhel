{
  "name": "Patch RHEL Servers",
  "description": "Options for patching RHEL web servers",
  "spec": [
    {
      "question_name": "Security updates only?",
      "question_description": "When enabled, only security-related packages are updated.",
      "required": true,
      "type": "multiplechoice",
      "variable": "security_only",
      "choices": ["false", "true"],
      "default": "false"
    },
    {
      "question_name": "Reboot after patching?",
      "question_description": "Reboot hosts when kernel or other updates require it (needs-restarting -r).",
      "required": true,
      "type": "multiplechoice",
      "variable": "reboot_after_patch",
      "choices": ["false", "true"],
      "default": "false"
    },
    {
      "question_name": "Prepare demo updates first?",
      "question_description": "Downgrade host-specific packages so the patch job has visible pending updates (idempotent; skips packages already pending).",
      "required": true,
      "type": "multiplechoice",
      "variable": "seed_demo_packages",
      "choices": ["false", "true"],
      "default": "false"
    }
  ]
}
